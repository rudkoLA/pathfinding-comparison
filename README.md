# Дослідження Алгоритмів Пошуку Шляху

Код містить набір функцій повʼязаних з графом, включаючи алгоритми для пошуку найкоротшого шляху в графі, створення візуалізації і графічного інтерфейсу.

# Algorithms
# 1. Breadth-First Search (BFS)
BFS використовується для пошуку найкоротшого шляху в графах. Алгоритм ітеративно досліджує всі вершини на одному рівні відстані від початкової вершини, поки не досягне кінцевої вершини.

Переваги:
- Простота реалізації.
- Гарантоване знаходження найкоротшого шляху в невагомих графах.

Недоліки:
- Погана продуктивність для великих графів через необхідність зберігати всі вершини поточного рівня у черзі.

# 2. Depth-First Search (DFS)
DFS заглиблюється в граф, обираючи один із можливих шляхів до кінця, потім повертається назад для перевірки інших. Для пошуку найкоротшого шляху необхідна додаткова логіка порівняння шляхів.

Переваги:
- Ефективний для графів з малою глибиною.
- Простота реалізації для пошуку будь-якого шляху.

Недоліки:
- Не гарантує найкоротшого шляху без модифікацій.
- Може повторно перевіряти вузли.

# 3. Dijkstra
Dijkstra працює за жадібним принципом: кожен раз вибирає вузол з мінімальною відстанню, оновлюючи ваги суміжних вершин.

Переваги:
- Гарантований найкоротший шлях у графах з невід’ємними вагами.
- Ефективний з використанням черги з пріоритетами.

Недоліки:
- Не підтримує графи з негативними вагами.
- Часова складність зростає для густих графів.

# 4. A (A-star)*
A* використовує евристику для оцінки найкоротшого шляху, комбінуючи фактичну вартість шляху та оцінку відстані до цілі.

Переваги:
- Висока ефективність завдяки використанню евристики.
- Оптимальний і повний для коректно налаштованої евристики.

Недоліки:
- Складність залежить від якості евристики.
- Вимагає додаткових обчислень для оцінки відстані.


# 5. Bellman-Ford
Bellman-Ford ітеративно розслаблює ребра графу, дозволяючи працювати з негативними вагами.

Переваги:
- Працює з графами, що мають негативні ваги.
- Простота розуміння та реалізації.

Недоліки:
- Більша складність 𝑂(𝑉⋅𝐸), де 𝑉 — вершини, 𝐸 — ребра.
- Повільний для великих графів.

Звіт: Алгоритми пошуку найкоротшого шляху
Цей звіт описує основні алгоритми пошуку найкоротшого шляху в графі та їх особливості. Розглянемо роботу кожного алгоритму, його переваги та недоліки, а також проведемо їх порівняння.



# 6. Floyd-Warshall
Цей алгоритм знаходить найкоротші шляхи між усіма парами вершин у графі.

Переваги:
- Всі пари шляхів у одній реалізації.
- Простота для невеликих графів.

Недоліки:
- Висока складність 𝑂(𝑉^3).
- Вимогливий до пам’яті.

7. Shortest Path Faster Algorithm (SPFA)
SPFA оптимізує Bellman-Ford, використовуючи чергу для обробки вузлів.

Переваги:
- Швидший, ніж Bellman-Ford, для розріджених графів.
- Працює з негативними вагами.

Недоліки:
- Гірший у найгірших випадках (𝑂(𝑉⋅𝐸)).
- Складність реалізації.

# Порівняння
Вибір алгоритму залежить від структури графу та вимог до розрахунків.

- Для неглибоких графів підходить DFS. Або коли потрібно знайти будь-який шлях, а не найкоротший.
- BFS краще підходить для незважених графів.
- Для графів із позитивними вагами найкращим вибором є Dijkstra або A*.
- Для графів із негативними вагами слід використовувати Bellman-Ford або SPFA.
- Floyd-Warshall ефективний для знаходження шляхів між усіма парами вершин. Проте потрібно враховувати що він працює найдовше.


# Graphical user interface (GUI):
Завантаження та генерація графa:
Користувач може завантажити граф із файлу або створити випадковий.
Програма підтримує як орієнтовані, так і неорієнтовані графи.

Input:
- Поля для введення вказівки початкового та кінцевої вершини.
- Перемикання на орієнтований з неорієнтованого і навпаки

Output:
- Відображає результати, включаючи знайдені шляхи, їх вагу та час виконання.
- Включає візуалізацію та графіки порівняння часу виконання.

# Visualization:
- Графіки візуалізуються за допомогою NetworkX і Matplotlib.
- Підтримує як орієнтовані, так і неорієнтовані графи.

# Graph Utilities:
# 1. read_graph_from_file(filename)

Призначення: Зчитує файл і повертає список ребер графа (edge_list) у форматі списку кортежів (a, b, weight).

Логіка роботи:
- Відкриває файл для читання. Якщо файл не знайдено, виводить повідомлення про помилку.
- Ігнорує пусті рядки та рядки, що починаються з # (коментарі).
- Для рядків з двома значеннями (вузли a та b) додає вагу за замовчуванням 1.
- Перевіряє коректність формату рядка (рівно три значення).
- Перетворює вагу в числовий тип (тип float). У разі помилки виводить повідомлення про некоректний формат ваги.

Результат: Список ребер графа у вигляді кортежів.

# 2. build_graph(edge_list, directed=False)

Призначення: Перетворює список ребер у словникове представлення графа.

Логіка роботи:
- Для кожного ребра додає вершини та вагу в словник graph.
- Якщо граф неорієнтований (directed=False), додає обидва напрямки ребра.

Результат: Словник, де ключі — вершини, а значення — списки сусідніх вершин із вагами.

# 3. graph_to_edge_list(graph, directed=False)

Призначення: Перетворює граф у словниковій формі назад у список ребер.

Логіка роботи:
- Для орієнтованого графа додає кожне ребро як є.
- Для неорієнтованого графа уникає дублювання ребер, використовуючи множину seen для зберігання унікальних пар вершин.

Результат: Список кортежів ребер у форматі (a, b, weight).

# Timing:
-Відстежує час виконання кожного алгоритму для порівняння продуктивності.

# Поділ завдань в команді:
- Каплиш Ольга: зробила функції для пошуку найкорошого шляху в графах згідно з алгоритмами DFS, BFS. Також брала участь в створенні презентації, писала звіт.
- Полянський Іван: зробив інтерфейс програми та функцію для пошуку найкорошого шляху в графах згідно з алгоритмом Dijkstra, A*.
- Петречко Анастасія: зробила функцію для пошуку найкорошого шляху в графах згідно з алгоритмом Floyd Marshall, Bellman Ford. І брала участь в створенні презентації.
- Рудько Адам: зробив функцію для пошуку найкорошого шляху в графах згідно з алгоритмом SPFA, фіксив баги.
- Мороз Тетяна: зробила функцію для генерації графу. Брала участь в створенні презентації, допомагала писати звіт.

